# Runtime dependencies.
# boto3/botocore ship with the Lambda Python runtime, so the deployment package
# stays empty of third-party code and cold starts stay small. Pinned here only
# so local runs match what Lambda provides.
boto3>=1.34,<2.0
